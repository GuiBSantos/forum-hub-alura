spring.application.name=Forum_Hub
spring.datasource.url=jdbc:mysql://localhost:3306/forum_hub
spring.datasource.username=root
spring.datasource.password=88675819

api.security.token.secret=${JWT_SECRET:12345678}

# Configura para que os rastros de erro (stacktrace) n�o sejam exibidos nas respostas de erro HTTP (por seguran�a)
server.error.include-stacktrace=never
# Faz com que o Hibernate exiba os comandos SQL no console (�til para debug e ver o que est� sendo executado)
spring.jpa.show-sql=true
# Configura o comportamento de cria��o e atualiza��o do schema pelo Hibernate:
# 'update' significa que ele tentar� atualizar a estrutura do banco automaticamente (n�o recomendado para produ��o)
spring.jpa.hibernate.ddl-auto=update
# Indica ao Hibernate que o SQL exibido no console deve ser formatado (indentado e leg�vel)
spring.jpa.properties.hibernate.format_sql=true
# Define qual dialeto do Hibernate deve ser usado para gerar SQLs compat�veis com o MariaDB
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.flyway.baseline-on-migrate=true
spring.flyway.repair-on-startup=true


